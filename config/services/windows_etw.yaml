service_name: windows_etw
title: Custom Windows ETW logs
description: Collect and parse logs from any Windows ETW provider with Elastic Agent.
service_info:
  common_use_cases:
  - '# TODO: Add common use cases for this service'
  data_types_collected:
  - '# TODO: Add data types collected (logs, metrics, traces, etc.)'
  compatibility:
    elastic_stack_versions:
    - ^8.17.0 || ^9.0.0
    service_versions:
    - '# TODO: Add compatible service versions'
  scaling_and_performance:
    description: '# TODO: Add service performance description'
    performance_expectations:
    - '# TODO: Add typical performance expectations'
    scaling_guidance:
    - '# TODO: Add scaling recommendations'
setup_instructions:
  prerequisites:
  - '# TODO: Add service prerequisites'
  installation_steps:
  - step: 1
    title: Install Custom Windows ETW logs
    description: '# TODO: Add Custom Windows ETW logs installation instructions'
    commands:
    - '# TODO: Add commands to install Custom Windows ETW logs'
kibana_setup_instructions:
  default:
    steps:
    - step: 1
      instruction: Navigate to 'Management' > 'Integrations' in Kibana.
    - step: 2
      instruction: Search for 'Custom Windows ETW logs'.
    - step: 3
      instruction: Select and add the 'Custom Windows ETW logs' integration.
    - step: 4
      instruction: Install Elastic Agent on the systems running Custom Windows ETW logs, if necessary.
    - step: 5
      instruction: '# TODO: Add service-specific configuration steps'
    - step: 6
      instruction: Press 'Save Integration' to start collecting data.
troubleshooting:
  common_issues:
  - issue: Custom Windows ETW logs fails to start
    solution: '# TODO: Add troubleshooting steps for Custom Windows ETW logs startup issues'
  - issue: No data being collected
    solution: '# TODO: Add troubleshooting steps for data collection issues'
validation_steps:
  steps:
  - step: 1
    title: Check Custom Windows ETW logs Service Status
    description: Verify that Custom Windows ETW logs is running and healthy
    commands:
    - '# TODO: Add commands to check Custom Windows ETW logs status'
    expected_output: '# TODO: Add expected output'
  - step: 2
    title: Check Integration Health
    description: Verify Elastic Agent is collecting data
    commands:
    - '# Check Elastic Agent logs for windows_etw integration'
    - '# Verify data appears in Elasticsearch indices'
    expected_output: No errors in agent logs, data visible in Kibana
documentation_sites:
- '# TODO: Add relevant documentation URLs'
