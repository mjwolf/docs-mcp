service_name: security_detection_engine
title: Prebuilt Security Detection Rules
description: Prebuilt detection rules for Elastic Security
service_info:
  common_use_cases:
  - '# TODO: Add common use cases for this service'
  data_types_collected:
  - '# TODO: Add data types collected (logs, metrics, traces, etc.)'
  compatibility:
    elastic_stack_versions:
    - ^9.2.0
    service_versions:
    - '# TODO: Add compatible service versions'
  scaling_and_performance:
    description: '# TODO: Add service performance description'
    performance_expectations:
    - '# TODO: Add typical performance expectations'
    scaling_guidance:
    - '# TODO: Add scaling recommendations'
setup_instructions:
  prerequisites:
  - '# TODO: Add service prerequisites'
  installation_steps:
  - step: 1
    title: Install Prebuilt Security Detection Rules
    description: '# TODO: Add Prebuilt Security Detection Rules installation instructions'
    commands:
    - '# TODO: Add commands to install Prebuilt Security Detection Rules'
kibana_setup_instructions:
  default:
    steps:
    - step: 1
      instruction: Navigate to 'Management' > 'Integrations' in Kibana.
    - step: 2
      instruction: Search for 'Prebuilt Security Detection Rules'.
    - step: 3
      instruction: Select and add the 'Prebuilt Security Detection Rules' integration.
    - step: 4
      instruction: Install Elastic Agent on the systems running Prebuilt Security Detection Rules, if necessary.
    - step: 5
      instruction: '# TODO: Add service-specific configuration steps'
    - step: 6
      instruction: Press 'Save Integration' to start collecting data.
troubleshooting:
  common_issues:
  - issue: Prebuilt Security Detection Rules fails to start
    solution: '# TODO: Add troubleshooting steps for Prebuilt Security Detection Rules startup issues'
  - issue: No data being collected
    solution: '# TODO: Add troubleshooting steps for data collection issues'
validation_steps:
  steps:
  - step: 1
    title: Check Prebuilt Security Detection Rules Service Status
    description: Verify that Prebuilt Security Detection Rules is running and healthy
    commands:
    - '# TODO: Add commands to check Prebuilt Security Detection Rules status'
    expected_output: '# TODO: Add expected output'
  - step: 2
    title: Check Integration Health
    description: Verify Elastic Agent is collecting data
    commands:
    - '# Check Elastic Agent logs for security_detection_engine integration'
    - '# Verify data appears in Elasticsearch indices'
    expected_output: No errors in agent logs, data visible in Kibana
documentation_sites:
- '# TODO: Add relevant documentation URLs'
