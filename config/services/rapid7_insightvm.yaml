service_name: rapid7_insightvm
title: Rapid7 InsightVM
description: Collect logs from Rapid7 InsightVM with Elastic Agent.
service_info:
  common_use_cases:
  - '# TODO: Add common use cases for this service'
  data_types_collected:
  - '# TODO: Add data types collected (logs, metrics, traces, etc.)'
  compatibility:
    elastic_stack_versions:
    - ^8.19.4 || ^9.1.4
    service_versions:
    - '# TODO: Add compatible service versions'
  scaling_and_performance:
    description: '# TODO: Add service performance description'
    performance_expectations:
    - '# TODO: Add typical performance expectations'
    scaling_guidance:
    - '# TODO: Add scaling recommendations'
setup_instructions:
  prerequisites:
  - '# TODO: Add service prerequisites'
  installation_steps:
  - step: 1
    title: Install Rapid7 InsightVM
    description: '# TODO: Add Rapid7 InsightVM installation instructions'
    commands:
    - '# TODO: Add commands to install Rapid7 InsightVM'
kibana_setup_instructions:
  default:
    steps:
    - step: 1
      instruction: Navigate to 'Management' > 'Integrations' in Kibana.
    - step: 2
      instruction: Search for 'Rapid7 InsightVM'.
    - step: 3
      instruction: Select and add the 'Rapid7 InsightVM' integration.
    - step: 4
      instruction: Install Elastic Agent on the systems running Rapid7 InsightVM, if necessary.
    - step: 5
      instruction: '# TODO: Add service-specific configuration steps'
    - step: 6
      instruction: Press 'Save Integration' to start collecting data.
troubleshooting:
  common_issues:
  - issue: Rapid7 InsightVM fails to start
    solution: '# TODO: Add troubleshooting steps for Rapid7 InsightVM startup issues'
  - issue: No data being collected
    solution: '# TODO: Add troubleshooting steps for data collection issues'
validation_steps:
  steps:
  - step: 1
    title: Check Rapid7 InsightVM Service Status
    description: Verify that Rapid7 InsightVM is running and healthy
    commands:
    - '# TODO: Add commands to check Rapid7 InsightVM status'
    expected_output: '# TODO: Add expected output'
  - step: 2
    title: Check Integration Health
    description: Verify Elastic Agent is collecting data
    commands:
    - '# Check Elastic Agent logs for rapid7_insightvm integration'
    - '# Verify data appears in Elasticsearch indices'
    expected_output: No errors in agent logs, data visible in Kibana
documentation_sites:
- '# TODO: Add relevant documentation URLs'
